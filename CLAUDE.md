# CLAUDE.md — プロジェクトルール

このファイルは Claude Code が本プロジェクトで作業する際の共通ルールです。すべての作業で遵守してください。

## プロジェクト概要

建設業向けの工程表（ガントチャート）を含む業務アプリ。
工程表は日曜・祝日を「休工日」として扱い、タスクバーは実働日数ベースで休日を自動スキップする。

## 技術スタック

- Vite + React + TypeScript
- Tailwind CSS
- Firebase (Firestore / Auth)
- react-router-dom / lucide-react
- テスト: Vitest

## コマンド

```bash
npm run dev          # 開発サーバー起動
npx tsc --noEmit     # 型チェック（コード変更後は必ず実行すること）
npx vitest run       # ユニットテスト実行（コード変更後は必ず実行すること）
```

## 作業ルール

1. **修正1件 = 1コミット。** 指示書に番号付きの修正がある場合、1番号ごとにコミットし、コミットメッセージに番号を含める（例: `fix(schedule): 修正1 ドラッグ中の表示範囲を凍結`）。
2. **コード変更後は必ず `npx tsc --noEmit` と `npx vitest run` を実行**し、両方パスしてからコミットする。失敗した場合は自分で修正してから報告する。
3. 大きな変更（複数ファイルにまたがる変更、構造変更）は、**先に実装計画を提示してユーザーの承認を得てから**実装する。

## 禁止事項

- 指示されていない箇所のリファクタリング・整形・命名変更・コメント削除
- ユーザーの承認なしの新規ライブラリ追加
- デザイン（色・余白・角丸・フォント等）の変更
- localStorage / sessionStorage の使用
- 日本語コメントの削除・英語化
- 本番 Firestore への書き込みを伴うテスト・検証（検証は Firebase Emulator または検証用プロジェクトで行う）

## 工程表に関する重要な仕様

- 休工日 = 日曜 + 日本の祝日（`src/utils/workdays.ts` の `isOff` が唯一の判定箇所。休日判定のロジックを他の場所に重複実装しないこと）
- 祝日データは **2025〜2027年** の静的テーブル（`HOLIDAYS`）。範囲を変更したら `HOLIDAY_YEAR_MIN` / `HOLIDAY_YEAR_MAX` も必ず更新する
- タスクの `duration` は暦日ではなく**実働日数**
- タスクの日付は `YYYY-MM-DD` 文字列で保持する
- 印刷は A4 横（landscape）前提。`.no-print` クラスの要素は印刷時非表示
- 営業日計算（`workdaySpan` / `countWorkdays` 等）を変更した場合は、対応するテスト（`workdays.test.ts`）も必ず更新し、退行がないことを確認する

---

# CLAUDE.md 追補：作業の進め方と報告の規律

これまでの作業で実際に問題になった点を、ルールとして固定する。すべての作業で遵守する。

## 報告の正直さ（最重要）

- **「検証した」「確認した」「テストした」と書くときは、それがコード上の論理確認なのか、実際に動かした実機確認なのかを必ず明示する。** 例：「コードのロジック上は機能するはず（実機未確認）」と「ブラウザで実際に操作して確認した」を区別する。曖昧に「検証済み」と断言しない。
- 実機確認・別端末確認・印刷確認など、自分（Claude Code）に実施手段がないものは「実施できない。ユーザーに依頼する」と正直に書く。できないことを「やった」と書かない。
- 自信がない箇所は「推測」「未確認」と明記する。断定と推測を混ぜない。

## コマンド出力は生のまま

- `git diff` / `git log` / `cat` / `tsc` 等の出力を提示するときは、**記憶から再構成せず、実際に実行したコマンドの生出力をそのまま貼る。** 編集・要約・「だいたいこう」を禁止する。
- 差分や全文の確認を求められたら、該当コマンドを実際に実行して出力を貼る。

## デプロイと破壊的操作のゲート

- **デプロイ（firebase deploy 等）は、ユーザーの明示的な承認を得てから実行する。** 「検証OKの報告 → ユーザーの承認 → デプロイ」の順を必ず守る。検証が済んでいても、承認なしにデプロイへ進めない。
- 実機検証が必要な変更は、デプロイ前に検証手順をユーザーに提示し、結果を待つ。自分で「コード確認したので大丈夫」と判断してデプロイしない。
- Firestoreルール変更・データ移行・ファイル削除・本番への書き込みを伴う操作は、実行前に影響範囲を説明し承認を得る。
- セキュリティルールやデータ構造を変更する場合、変更後の全文をユーザーが確認できる形で提示してから適用する（diff の一部だけで済ませない）。

## スコープの厳守

- 指示された範囲だけを変更する。指示にない機能の追加・削除・リファクタリング・命名変更・整形をしない。
- 既存機能（特に他の人／別タスクで実装された機能）を、今回のスコープ外なら絶対に削除・改変しない。判断に迷ったら手を止めて確認する。
- 「ついでに直す」「より良くしておく」を勝手にやらない。気づいた別の問題は、実装せずに「別途こういう問題がある」と報告するだけにとどめる。

## データ消失の予防

- ローカル保存（localStorage等）とクラウド保存（Firestore等）の両方がある場合、「ローカルにあるがクラウドに無い」状態が画面切替・再読込・別端末で消える経路がないかを常に意識する。
- クラウドからの読み込みでローカルの未保存データを上書きする処理には、タイムスタンプ等で「新しい方を残す」ガードが要る。無条件上書きを避ける。
- 編集直後にアンマウント・画面離脱すると保存が飛ぶ経路（デバウンス保存のフラッシュ漏れ）に注意する。

## 1件ずつ・検証してから次へ

- 1修正＝1コミット。複数の指摘をまとめて一気に実装しない。
- 各コミットの前に `npx tsc --noEmit`（エラー0件）と、対象に関わるテストがあれば `npx vitest run` を実行し、両方パスを確認してからコミットする。
- 設計判断を要する変更・複数ファイルにまたがる変更・データやセキュリティに関わる変更は、**先に実装計画（方針）を提示して承認を得てから**着手する。コードをいきなり書かない。

## 謎の変更を残さない

- 自分が作成した覚えのないコミット・自動生成された変更に気づいたら、勝手に放置せず、内容（`git show <hash> --stat` 等）をユーザーに報告する。
- 作業ツリーは把握可能な状態に保つ。意図不明な変更を抱えたまま次の作業に進まない。

---

## 補足：ユーザーとの役割分担（参考）

- Claude Code は実装と、コードに基づく事実確認（grep・ログ・型チェック）を担う。
- 実機での動作確認・別端末確認・印刷確認・本番デプロイの承認は、ユーザーが行う。
- 大きな設計判断やリスク評価は、ユーザー（および相談相手）のレビューを経る。
- これらの境界を踏み越えて「やったことにする」のが最も避けるべき失敗である。
