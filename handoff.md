# ハンドオフメモ — 2026-07-13（最終）

## ステータスサマリー

| 機能 | 状態 |
|------|------|
| 回転写真の縮小不具合（写真台帳） | ✅ 完了・本番デプロイ済み・実機確認済み |
| 工事種別テンプレート → 写真への基準値反映 | ✅ 完了・本番デプロイ済み・実機確認済み |
| 写真台帳 説明欄の文字を大きく・読みやすく | ✅ 完了・本番デプロイ済み・実機確認済み |
| 写真台帳 工程名のフォントサイズ拡大 | ✅ 完了・本番デプロイ済み・実機確認済み |

**本番 URL: https://kawara-photo-app.web.app**

---

## 本番デプロイ済みコミット一覧（今日分）

| コミット | 内容 |
|---------|------|
| `9ceb14a` | 写真台帳: contain 表示・No.バッジ削除・番号を情報欄へ移動 |
| `5c1a719` | 写真台帳: 写真を高さ基準 4:3 に変更・空白解消 |
| `d45f95a` | 写真台帳: 回転写真修正（1回目） |
| `7539a65` | 写真台帳: 回転写真修正（2回目） |
| `f331956` | 写真台帳: 回転写真修正（**解決**・実機確認済み） |
| `5bae854` | types.ts: `WorkTypeTemplateItem`・`PhotoMaster` に `standard` 追加 |
| `4643bb4` | SettingsPage: 工事種別テンプレートに基準値入力欄追加 |
| `8b941ee` | PhotoPage: `applyPhotoMaster` で `standard` を写真に反映 |
| `8d3c63e` | SettingsPage: 写真テンプレートマスタに基準値入力欄追加 |
| `80ee1d2` | SettingsPage: 一括反映で Firestore に即時保存（**根本修正**） |
| `8af8fe5` | 写真台帳: 説明欄 13px・炭色・行間 1.7（実機確認済み） |
| `50895c2` | 写真台帳: 工程名 14px / 印刷 9pt に拡大（実機確認済み） |

---

## 機能詳細

### 1. 回転写真の縮小不具合（✅ 完了）

写真台帳で 90°/270° 回転した写真が非回転写真より小さく表示されていた不具合。
コンテナを常に `aspectRatio: '4/3'` 固定にし、内側 div を `width:75%, height:133.333%` + `rotate(90deg)` で数学的に解決。

### 2. 工事種別テンプレート → 写真への基準値反映（✅ 完了）

**業務フロー（完成）：**
```
設定 → 工事種別テンプレートに工程・基準値を入力
     → 「一括反映」ボタン → Firestore に即時保存（保存ボタン不要）
写真編集 → 「テンプレート」ボタン → マスタ選択
         → 工程・基準値・説明文が写真に自動入力
```

**根本原因だった箇所：** `applyWorkTypeTemplate` が React state しか更新せず Firestore に書かなかった。
「一括反映」後に「保存」を別途押さないと次回ロード時に古いデータが読まれていた。
→ async 化して即時 Firestore 保存に修正（`80ee1d2`）。

### 3. 写真台帳 文字サイズ調整（✅ 完了）

| 対象 | 変更前 | 変更後 |
|------|-------|-------|
| 説明欄 fontSize (screen) | 11px | 13px |
| 説明欄 fontSize (print) | 7.5pt | 8.9pt |
| 説明欄 color | #33383d | #1c1f22（炭色） |
| 説明欄 lineHeight | 1.4 | 1.7 |
| 工程名 fontSize (screen) | 12.5px | 14px |
| 工程名 fontSize (print) | 8pt | 9pt |

---

## 残タスク（次回以降）

- ブランチ `fix/schedule-page` → `main` へのマージ・PR 作成が未実施
- `doSaveToPhotoMaster`（写真 → マスタへの逆登録）に `standard` を含めるか検討
  （現在は `process`・`description` のみ。写真に手入力した基準値をマスタにも保存したい場合に必要）

---

## 現在のブランチ

`fix/schedule-page`（`main` へのマージ未実施）
