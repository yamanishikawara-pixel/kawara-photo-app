# ハンドオフメモ — 2026-07-13（最終）

## ステータスサマリー

| 機能 | 状態 |
|------|------|
| 回転写真の縮小不具合（写真台帳） | ✅ 完了・デプロイ済み |
| 工事種別テンプレート → 写真への基準値反映 | ✅ 完了・デプロイ済み・実機確認済み |

---

## 1. 回転写真の縮小不具合

**✅ 直った（ユーザー実機確認済み）**

No.1・No.2（回転あり）が No.3（回転なし）と同じサイズになることを実機確認済み。

最終コミット: `f331956`

修正内容: 写真コンテナを常に `aspectRatio: '4/3'`（横長）で固定。
回転あり（90°/270°）の内側 div を `width: 75%, height: 133.333%` にして `rotate(90deg)` することで、
横長コンテナをちょうど埋めるよう数学的に解決（`PdfExportPage.tsx`）。

---

## 2. 工事種別テンプレート → 写真への基準値反映

**✅ 完了・デプロイ済み・実機確認済み**

### ユーザーの業務フロー

```
設定画面 → 工事種別テンプレートに工程・基準値を登録
         → 「一括反映」ボタン → Firestore に即時保存
写真編集画面 → 「テンプレート」ボタン → マスタ選択
         → 工程・基準値・説明文が写真に自動入力
```

### 実装したコミット

| コミット | 内容 |
|---------|------|
| `5bae854` | `WorkTypeTemplateItem`・`PhotoMaster` に `standard?: string` 追加 |
| `4643bb4` | 工事種別テンプレート行に基準値入力欄追加。`applyWorkTypeTemplate` で `standard` を photoMaster に含めるよう修正 |
| `8b941ee` | `applyPhotoMaster` で `standard` も写真に反映 |
| `8d3c63e` | 写真テンプレートマスタの直接編集 UI に基準値入力欄追加 |
| `80ee1d2` | **根本修正**: `applyWorkTypeTemplate`（一括反映）を async 化し Firestore に即時保存。「保存」ボタンを別途押す必要がなくなった |

### 根本原因だった箇所

`applyWorkTypeTemplate` が React の local state しか更新せず Firestore に書いていなかった。
「一括反映」後に「保存」ボタンを押さないと写真編集画面が古いデータを読み込み、基準値が反映されなかった。

---

## 3. 本番デプロイ済みコミット一覧

本番 URL: https://kawara-photo-app.web.app

| コミット | 内容 |
|---------|------|
| `9ceb14a` | 写真台帳: contain 表示・No.バッジ削除・番号を情報欄へ移動 |
| `5c1a719` | 写真台帳: 写真を高さ基準 4:3 に変更・空白解消 |
| `d45f95a` | 写真台帳: 回転写真修正（1回目） |
| `7539a65` | 写真台帳: 回転写真修正（2回目） |
| `f331956` | 写真台帳: 回転写真修正（**解決**） |
| `5bae854` | types.ts: standard フィールド追加 |
| `4643bb4` | SettingsPage: 工事種別テンプレートに基準値入力欄 + applyWorkTypeTemplate 修正 |
| `8b941ee` | PhotoPage: applyPhotoMaster で standard を反映 |
| `8d3c63e` | SettingsPage: 写真テンプレートマスタに基準値入力欄追加 |
| `80ee1d2` | SettingsPage: 一括反映で Firestore に即時保存（根本修正） |

---

## 4. 次回やること

### 残タスク（中長期）

- ブランチ `fix/schedule-page` → `main` へのマージ・PR 作成が未実施
- `doSaveToPhotoMaster`（写真 → マスタへの逆登録）に `standard` を含めるか検討
  （現在は `process`・`description` のみ保存。実害はないが、写真に手入力した基準値をマスタに保存したい場合は追加が必要）

---

## 現在のブランチ

`fix/schedule-page`（`main` へのマージ未実施）
